"la diferencia entre public y private en variables y clases es quien puede modificarlas externamente"
"entonces aqui ya no se usan naturalmente las impresiones normales para saber el saldo de algo, o la asignacion simple"
"para que hicimos estas dos cuentas si nunca las ocupamos" (getters de Banco)

"que es un getter y un setter"
"que hace el get y que hace el set"
"por que se hace primero el get saldo y luego el set saldo"
"entonces, primero se crean los getters y los setters y despues los constructores"
"por que se pone el !"

"que es el metodo toString personalizado"
"como funciona exactamente el toString, en que momento le estamos diciendo que si le hacemos un print(cuenta) se imprime todo"

"se puede hacer el return de un print"
"si voy a buscar una cuenta necesito que mi funcion sea tipo string"
"por que la funcion tiene que ser de tipo Cuenta"

En resumen desde el historial de prompts  le pregunte por cada parte de la practica, como acceder a datos protegidos o 
privados por medio de getters y setters, como meter validaciones en los setters y darme cuenta que probablemente siempre
de aqui en delante vaya a trabajar con ellos por mera seguridad y por que cuando trabaje o programe para empresas no
creo que dejen sus variables asi tan frescas para que cualquiera las modifique, le pregunte dudas sobre como 
funcionan los returns en java y si los operadores relacionales eran iguales que en otros lenguajes.
Aparte pregunte como determinar el tipo de retorno sobretodo por que tenia duda de como devolver un tipo Cuenta

como la clase Banco estaba compuesta con Cuenta, pues naturalmente dentro de las operaciones de banco un metodo
usaba el tipo cuenta para retornar.